import streamlit as st
import google.generativeai as genai
import os

# --- 1. การตั้งค่าเบื้องต้น ---
# วาง API Key ของคุณตรงนี้ได้เลย (หรือจะใช้ st.secrets["GEMINI_API_KEY"] ก็ได้)
API_KEY = "วาง_API_KEY_จริงของคุณที่นี่" 
genai.configure(api_key=API_KEY)

# ตั้งค่าหน้าแอปและไอคอน
st.set_page_config(page_title="Synapse - อยู่นิ่งๆ ไม่เจ็บตัว", layout="centered")

# --- 2. CSS ตกแต่ง (โทนสี แดง น้ำเงิน เขียว ขาว เข้มสุดๆ) ---
st.markdown("""
    <style>
    /* พื้นหลังสีน้ำเงินเข้ม */
    .stApp { background-color: #0000FF; color: #FFFFFF; }
    
    /* หัวข้อสีขาว ขอบแดง */
    h1, h2, h3 { color: #FFFFFF; text-shadow: 4px 4px #FF0000; text-align: center; }
    
    /* ปุ่มสีแดง ตัวหนังสือขาว */
    .stButton>button { 
        background-color: #FF0000; color: white; border: 3px solid #FFFFFF;
        width: 100%; font-weight: bold; font-size: 22px; height: 60px;
    }
    
    /* ช่องพิมพ์ข้อความ สีขาว ตัวหนังสือดำ */
    div[data-testid="stTextArea"] textarea { background-color: #FFFFFF; color: #000; border: 5px solid #FF0000; font-size: 18px; }
    
    /* กล่องข้อความแชท */
    .stChatMessage { background-color: #008000; border: 2px solid #FFFFFF; }
    </style>
    """, unsafe_allow_html=True)

# --- 3. ฟังก์ชันหลักของแอป ---

# แสดงโลโก้ (ถ้ามีไฟล์ logo.jpg ในโฟลเดอร์)
if os.path.exists("logo.jpg"):
    st.image("logo.jpg", width=150)
else:
    st.write("### 🧠 SYNAPSE")

# เมนูหน้าจอ 3 หน้าหลัก
tab1, tab2, tab3 = st.tabs(["💬 ระบาย & แต่งเพลง", "📂 ผลลัพธ์ & ดาวน์โหลด", "🤖 ปรึกษา AI"])

# --- หน้า 1: ระบายความในใจ & เลือกแนวเพลง ---
with tab1:
    st.subheader("1. กระดานข้อความละบายความในใจ")
    user_input = st.text_area("ละบายความในใจตามที่ผู้ใช้ต้องการ...", height=200, placeholder="พิมพ์ที่นี่...")
    
    st.markdown("---")
    st.subheader("2. เลือกแนวเนื้อเพลง")
    genre = st.selectbox("เลือกแนวเพลงที่ต้องการขยายใจความ:", 
                        ["Pop", "Rock", "R&B", "Rap", "Hip-hop", "ลูกทุ่ง", "เพื่อชีวิต", "หมอลำ"])
    
    if st.button("✨ ให้ 'อยู่นิ่งๆ' แต่งเพลงให้"):
        if user_input:
            try:
                model = genai.GenerativeModel('gemini-1.5-flash')
                prompt = f"คุณคือ AI ชื่อ 'อยู่นิ่งๆไม่เจ็บตัว' จงนำข้อความนี้: '{user_input}' มาแต่งเป็นเพลงแนว {genre} ที่ได้ใจความลึกซึ้ง มีเนื้อร้องพร้อมคอร์ดและคีย์เพลงประกอบ"
                
                with st.spinner('กำลังประมวลผล...'):
                    response = model.generate_content(prompt)
                    st.session_state.lyrics = response.text
                    st.success("แต่งเพลงเสร็จแล้ว! ไปดูที่หน้า 'ผลลัพธ์' ได้เลย")
            except Exception as e:
                st.error(f"เกิดข้อผิดพลาด: {e}")
        else:
            st.warning("กรุณาพิมพ์ระบายความในใจก่อนครับ")

# --- หน้า 2: เนื้อเพลงจาก AI & ระบบดาวน์โหลด ---
with tab2:
    st.subheader("3. เนื้อเพลงพร้อมคอร์ดและคีย์")
    if 'lyrics' in st.session_state:
        st.code(st.session_state.lyrics, language="text")
        
        st.markdown("---")
        st.subheader("4. ปุ่มดาวน์โหลดข้อมูล")
        col1, col2 = st.columns(2)
        
        with col1:
            if st.button("📢 กดแชร์ (บังคับก่อนโหลด)"):
                st.session_state.shared = True
                st.balloons()
                st.success("แชร์สำเร็จ! ปุ่มดาวน์โหลดเปิดใช้งานแล้ว")
        
        with col2:
            if st.session_state.get('shared', False):
                st.download_button("💾 ดาวน์โหลดไฟล์เพลง (.txt)", 
                                 data=st.session_state.lyrics, 
                                 file_name="synapse_song.txt")
            else:
                st.button("💾 ดาวน์โหลด (ต้องกดแชร์ก่อน)", disabled=True)
    else:
        st.info("ยังไม่มีเนื้อเพลง กรุณาไปแต่งเพลงที่หน้าแรกก่อน")

# --- หน้า 3: ปรับทุกข์คุยกับ AI ---
with tab3:
    st.subheader("หน้าเสริม 4. ปรับทุกข์คุยกับ AI")
    st.write("ชื่อ AI: **อยู่นิ่งๆไม่เจ็บตัว** (คุยได้ทุกเรื่อง)")
    
    if "messages" not in st.session_state:
        st.session_state.messages = []

    for msg in st.session_state.messages:
        with st.chat_message(msg["role"]):
            st.write(msg["content"])

    if chat_p := st.chat_input("คุยกับ AI ที่นี่..."):
        st.session_state.messages.append({"role": "user", "content": chat_p})
        with st.chat_message("user"): st.write(chat_p)
        
        model = genai.GenerativeModel('gemini-1.5-flash')
        chat_res = model.generate_content(chat_p)
        
        st.session_state.messages.append({"role": "assistant", "content": chat_res.text})
        with st.chat_message("assistant"): st.write(chat_res.text)

# --- 5. เครื่องเล่นเพลง MP3 (เล่นวนตลอดเวลา) ---
st.sidebar.markdown("### 🎵 Synapse Player")
st.sidebar.write("เพลงจะเล่นวนไปตลอดเวลาไม่หยุด")

# ตรวจสอบไฟล์ mp3 ในโฟลเดอร์
mp3_file = "music.mp3" # แก้ชื่อให้ตรงกับไฟล์ของคุณ
if os.path.exists(mp3_file):
    with open(mp3_file, 'rb') as f:
        audio_bytes = f.read()
        st.sidebar.audio(audio_bytes, format="audio/mp3", loop=True)
else:
    st.sidebar.error("❌ ไม่พบไฟล์ music.mp3")
