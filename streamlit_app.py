import streamlit as st
from pydub import AudioSegment
import io

# 1. ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="Audio Mixer", page_icon="🎵")
st.header("เครื่องมือรวมเสียงร้อง + ดนตรี")
st.markdown("---")

# 2. ส่วนอัปโหลดไฟล์ (แก้ไขภาษาอังกฤษให้ถูกต้องเพื่อให้ระบบหาไฟล์เจอ)
vocal_file = st.file_uploader("1. อัปโหลดไฟล์เสียงร้อง (Vocal)", type=["wav", "mp3", "m4a"])
instrument_file = st.file_uploader("2. อัปโหลดไฟล์ดนตรี (Instrument)", type=["wav", "mp3", "m4a"])

# 3. ตรวจสอบว่าอัปโหลดครบหรือยัง
if vocal_file and instrument_file:
    st.success("ไฟล์พร้อมแล้วครับ!")
    
    # ปุ่มเริ่มทำงาน
    if st.button("🚀 เริ่มรวมเสียง (Merge Audio)"):
        with st.spinner("กำลังผสมเสียง... รอสักครู่นะครับ"):
            try:
                # โหลดไฟล์เสียง
                vocal = AudioSegment.from_file(vocal_file)
                inst = AudioSegment.from_file(instrument_file)

                # รวมเสียง (Overlay)
                # ถ้าเสียงร้องเบาไป สามารถเพิ่มความดังได้ที่นี่ เช่น vocal + 3
                combined = inst.overlay(vocal)

                # เตรียมไฟล์สำหรับส่งออก
                out = io.BytesIO()
                combined.export(out, format="wav")
                
                st.markdown("### ✨ รวมเสร็จเรียบร้อย!")
                # แสดงเครื่องเล่นเพลง
                st.audio(out) 
                
                # ปุ่มดาวน์โหลด
                st.download_button(
                    label="⬇️ ดาวน์โหลดเพลงที่เสร็จแล้ว",
                    data=out.getvalue(),
                    file_name="finished_song.wav",
                    mime="audio/wav"
                )
            except Exception as e:
                st.error(f"เกิดข้อผิดพลาด: {e}")
                st.info("คำแนะนำ: ตรวจสอบว่าไฟล์ที่อัปโหลดเป็นไฟล์เสียงที่เล่นได้ปกติ")

else:
    st.warning("กรุณาอัปโหลดไฟล์ให้ครบทั้ง 2 ช่อง (เสียงร้อง และ ดนตรี)")

st.markdown("---")
st.caption("สโลแกน: อยู่นิ่งๆ ไม่เจ็บตัว")
